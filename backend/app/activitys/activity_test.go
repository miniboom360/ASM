package activitys

import (
  "testing"
)

func TestActivity(t *testing.T) {
  // domains := make([]string, 0)
  // domains = append(domains, "chinapnr.com")
  //
  // res, err := SearchSubDomain(context.Background(), domains)
  // if err != nil {
  //   panic(err)
  // }
  // fmt.Printf("以下是子域名内容:%+v\n", res)
}
